% ------------------------------------------------------------------------
% file `khec-exercise-34-exercise-body.tex'
%
%     exercise of type `exercise' with id `34'
%
% generated by the `exercise' environment of the
%   `xsim' package v0.19a (2020/03/19)
% from source `khec' on 2020/11/05 on line 94
% ------------------------------------------------------------------------
Soit $c$, $a$, $i$ des réels positifs. On note $R_n = c(a-i)(1+i)^n$ pour tout $n\in \NN$.
	\begin{tasks}
		\task Calculer $S_n = \sum\limits_{k=0}^n R_n$
		\task On suppose que $S_n = e$. Calculer $a$ en fonction de $i$.
	\end{tasks}
